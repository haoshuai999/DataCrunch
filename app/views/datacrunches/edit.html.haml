- if params[:user_login]
  %p= params[:user_login]
  = form_tag datacrunches_path, :method => 'get' do
    = submit_tag 'Log out', :class => 'btn btn-primary', :id => 'logout'
%h1 Data Crunch
%h2 Edit Existing Datacrunches

= form_tag datacrunch_path(@datacrunches), :method => :put do

  = label :datacrunch, :title, 'Title'
  = text_field :datacrunch, 'title', placeholder: @datacrunches.title

  = label :datacrunch, :rating, 'Description'
  = text_field :datacrunch, 'description', placeholder: @datacrunches.description

  = label :datacrunch, :publicity, 'Publicity'
  = select :datacrunch, :publicity, [['public', true], ['private', false]]

  = submit_tag 'Update Datacrunch Info', :class => 'btn btn-primary'